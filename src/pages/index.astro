---
// Clean Operational Presentation Canvas for PanorOmics
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link rel="icon" type="image/png" href="/icon-mark.png" />
		<title>PanorOmics | Unifying Omics. Advancing Health. Transforming Lives.</title>
		<style>
			/* Strip browser defaults */
			html, body {
				margin: 0;
				padding: 0;
				box-sizing: border-box;
				background-color: #010610;
				color: #ffffff;
				font-family: system-ui, -apple-system, sans-serif;
				scroll-behavior: smooth;
				width: 100%;
				min-height: 100vh;
				overflow-x: hidden;
			}

			/* FIXED STICKY NAVIGATION BAR */
			.site-header {
				background-color: #f4f7fa; 
				border-bottom: 1px solid rgba(0, 51, 102, 0.1);
				position: sticky;
				top: 0;
				z-index: 100;
				width: 100%;
				padding: 0.75rem 0;
			}

			.nav-container {
				max-width: 1400px;
				margin: 0 auto;
				padding: 0 2rem;
				display: flex;
				justify-content: space-between;
				align-items: center;
			}

			.brand-link {
				display: flex;
				align-items: center;
				gap: 14px;
				text-decoration: none;
			}

			.header-logo-icon {
				height: 45px;
				width: auto;
				display: block;
				border-radius: 8px;
			}

			.text-identity {
				display: flex;
				flex-direction: column;
				line-height: 1.2;
			}

			.brand-text {
				font-size: 1.5rem;
				font-weight: 800;
				color: #00264d; 
				letter-spacing: -0.5px;
			}

			.brand-subtitle {
				font-size: 0.7rem;
				color: #4a607a; 
				font-weight: 600;
				text-transform: uppercase;
				letter-spacing: 0.5px;
				margin-top: 1px;
			}

			/* PAGE CONTENT LAYOUT */
			.landing-container {
				width: 100%;
				display: flex;
				flex-direction: column;
				align-items: center;
				background-color: #010610;
			}

			/* HORIZONTAL CODE-BASED HERO BANNER */
			.code-hero {
				width: 100%;
				max-width: 1400px;
				padding: 6rem 2rem;
				box-sizing: border-box;
				display: flex;
				align-items: center;
				justify-content: center;
				gap: 4rem;
			}

			/* Crops the square image down to just show the helix icon mark */
			.helix-crop-box {
				width: 280px;
				height: 280px;
				overflow: hidden;
				border-radius: 50%;
				display: flex;
				align-items: center;
				justify-content: center;
				flex-shrink: 0;
				box-shadow: 0 20px 50px rgba(0,0,0,0.3);
			}

			.helix-crop-box img {
				width: 160%;
				height: auto;
				transform: translateY(-55px);
			}

			.hero-text-block {
				display: flex;
				flex-direction: column;
				max-width: 600px;
			}

			.hero-headline {
				font-size: clamp(2.5rem, 5vw, 4rem);
				font-weight: 800;
				line-height: 1.1;
				margin: 0 0 1rem 0;
				letter-spacing: -1px;
			}

			.hero-headline span {
				color: #1a8cff;
			}

			.hero-slogan {
				font-size: 1.15rem;
				color: #a0c0e0;
				line-height: 1.6;
				margin: 0;
				letter-spacing: 0.3px;
				text-transform: lowercase;
			}

			/* IMAGE SECTIONS */
			.page-section {
				width: 100%;
				max-width: 1920px;
				display: flex;
				justify-content: center;
			}

			.full-page-asset {
				width: 100%;
				height: auto;
				display: block;
			}

			/* Responsive tweaks for mobile viewports */
			@media (max-width: 820px) {
				.code-hero {
					flex-direction: column;
					text-align: center;
					gap: 2rem;
					padding: 4rem 1.5rem;
				}
				.helix-crop-box {
					width: 200px;
					height: 200px;
				}
				.helix-crop-box img {
					transform: translateY(-38px);
				}
			}
		</style>
	</head>
	<body>

		<!-- HEADER BAR -->
		<header class="site-header">
			<div class="nav-container">
				<a href="/" class="brand-link">
					<img src="/icon-mark.png" alt="PanorOmics Icon" class="header-logo-icon" />
					
						<span class="brand-text">PanorOmics</span>
						<span class="brand-subtitle">The Panoramic Omics Sciences Center</span>
					</div>
				</a>
			</div>
		</header>

		<!-- INFOGRAPHIC SCROLLING SECTIONS -->
		<div class="landing-container">
			
			<!-- SECTION 1: CUSTOM WIDESCREEN CODE HERO BLOCK -->
			<section class="code-hero">
				<div class="helix-crop-box">
					<img src="/icon-mark.png" alt="PanorOmics Helix" />
				</div>
				<div class="hero-text-block">
					<h1 class="hero-headline">Panor<span>Omics</span></h1>
					<p class="hero-slogan">integrating life sciences • data • discovery</p>
				</div>
			</section>

			<!-- SECTION 2: CENTRAL DOGMA MAP -->
			<section class="page-section">
				<img 
					src="/central-dogma.png" 
					alt="The Central Dogma of Omics Science" 
					class="full-page-asset"
				/>
			</section>

		</div>
	</body>
</html>
